version: "2"

run:
  timeout: 5m

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - exhaustive
    - gocritic
    - misspell
    - nilerr
    - noctx
    - nolintlint
    - revive
    - unconvert

formatters:
  enable:
    - gofmt
    - goimports
